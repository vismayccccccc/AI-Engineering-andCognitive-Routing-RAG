# AI-Engineering-andCognitive-Routing-RAG
This project builds an AI agent that generates persona-driven social media posts using LLMs, LangGraph, and RAG. The system converts a persona into a search query, retrieves relevant context, and generates structured JSON output. It uses a vector database (ChromaDB) to store and retrieve past posts, enabling memory-aware responses. 
